# Указываем базовый образ
FROM node:20-alpine

# Создаем рабочую директорию
WORKDIR /app

# Копируем весь исходный код приложения в контейнер
COPY . .

# Устанавливаем pnpm
RUN npm install -g pnpm

# Устанавливаем зависимости на основе lock-файла
RUN pnpm i --force

# Собираем Nuxt приложение
RUN pnpm build

# Указываем, что контейнер будет слушать 3000 порт
EXPOSE 3000

# Запускаем приложение
CMD ["pnpm", "start"]
